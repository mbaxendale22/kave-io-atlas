import React from "react";

const Origin = ({ origin }) => {
  return (
    <div className=" text-contrast w-full h-full flex flex-col justify-center items-center font-Roboto text-sm">
      <h2>ORIGIN</h2>
      <p>{origin}</p>
    </div>
  );
};

export default Origin;
